extends layout

block content
  br
  .w3-cell-row
    .w3-container.w3-cell.w3-card-4.w3-hide-small(style="width:20%")
      br
      b(id="emailUser", style="text-align:center")= user.email
      hr
      p Welcome to the user front page, you can select multiple options as
        if(user.utype==2)
          span= " Consumer."
        else
          if(user.utype==1)
            span= " Producer."
          else 
            span= " Admin."
      hr
      if(user.utype==0)
        br
        a.w3-button(href="http://localhost:8000/admin") Admin View

    .w3-container.w3-cell
      h3 News
      ul.w3-ul.w3-card-4.w3-container(id="newsList")
      hr
      h3 Events
      ul.w3-ul.w3-card-4.w3-container(id="eventsList")
    

    script(src='/javascripts/User/home.js')